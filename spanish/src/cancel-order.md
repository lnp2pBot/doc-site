# Cancelando una orden

La otra alternativa que tiene el solver es cancelar la orden, si el caso lo amerita. Para ello usa el comando `/cancelorder`.

![Cancel order capture](./assets/images/cancelorder.png)